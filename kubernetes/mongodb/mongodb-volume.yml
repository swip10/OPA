apiVersion: v1
kind: PersistentVolume # Create PV 
metadata:
  name: mongodb-volume # Sets PV name
  labels:
    type: local # Sets PV's type
    app: mongodb
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi # Sets PV's size
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /run/desktop/mnt/host/c/Users/Ben/Documents/GitHub/OPA/docker/mongodb/db 
    type: Directory
